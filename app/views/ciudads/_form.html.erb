<%= form_with(model: ciudad, local: true) do |form| %>
  <% if ciudad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ciudad.errors.count, "error") %> prohibited this ciudad from being saved:</h2>

      <ul>
        <% ciudad.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :habitantes %>
    <%= form.text_field :habitantes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
